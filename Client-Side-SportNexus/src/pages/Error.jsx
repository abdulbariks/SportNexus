import React from "react";
import useTitle from "../hooks/useTitle";

const Error = () => {
  useTitle("Error | SportNexus ");
  return (
    <div>
      <h2>Error page</h2>
    </div>
  );
};

export default Error;
